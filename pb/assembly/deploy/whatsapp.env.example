# Fill locally on the Pi. Do not commit real credentials.
TWILIO_ACCOUNT_SID=REPLACE_WITH_ACCOUNT_SID
TWILIO_AUTH_TOKEN=REPLACE_WITH_AUTH_TOKEN
TWILIO_WHATSAPP_FROM=whatsapp:+REPLACE_WITH_TWILIO_SENDER
TWILIO_WHATSAPP_TO=whatsapp:+REPLACE_WITH_RECIPIENT
# Approved template without variables for notifications outside the 24h window.
# Leave empty only for a test inside the open customer service window.
TWILIO_CONTENT_SID=
