# Confirm this URL returns one image, not the web UI or video stream.
CAMERA_URL=http://192.168.1.110:8080/shot.jpg
# Initial value only: calibrate with full and empty bowl images.
THRESHOLD=100
CAPTURE_INTERVAL=2
# Optional crop in original image coordinates, e.g. --crop 640:480:0:0
CROP_ARGS=
